package live.anonymespy.optigestion

/**
 * Defines the user experience level.
 * PRO: Standard enterprise analytic accounting.
 * SIMPLE: Streamlined terminology for non-experts.
 */
enum class AppMode {
    SIMPLE,
    PRO
}
